// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  urlApi: "http://movie0706.cybersoft.edu.vn/api/",
  firebase: {
    apiKey: "...",
    authDomain: "superfilm95.firebaseapp.com",
    databaseURL: "https://superfilm95.firebaseio.com",
    projectId: "superfilm95",
    storageBucket: "",
    messagingSenderId: "767785170040",
    appId: "1:767785170040:web:6055c095d11b82d7ec698d"
  }
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
